var should = require('chai').should();
var request = require('supertest');

describe('Admin Controller', function() {
    it('should return name of the database in', function () {
        true.should.be.true;
    });
});